using Microsoft.AspNetCore.Mvc;

namespace Project.Controllers
{
    public class ActiveDeviceController : Controller
    {
        
    }
}

//    // Get Scanner Id from request header that found this device
//    var scannerIdSet = Request.Headers.TryGetValue("Scanner-Id", out var scannerId);